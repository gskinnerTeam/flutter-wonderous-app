import 'package:wonders/common_libs.dart';

class AppLoadError extends StatelessWidget {
  const AppLoadError({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Center(child: Icon(Icons.warning));
}
