import 'package:wonders/common_libs.dart';

class WondersIntroScreen extends StatelessWidget {
  const WondersIntroScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
